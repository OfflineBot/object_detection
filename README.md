This is an object-detection Neural Network implemented with the `pytorch` library.

It implements a class for pulling from the dataset defined in `data.json` inside the `dataset` folder. <br>
(It's not done yet)

